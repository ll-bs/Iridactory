//大概是一个 新版本的元胞自动机，只是规则变得更多（但不一定就变得复杂了） 打算借用自走棋的那些概念 比如羁绊什么的 来参与到游戏里面(主要是方便玩家理解) 

//起初我只是有一个想法 有一个牧场 上面有许多独角兽工作 最后产出彩虹， （考虑mcmod核电工艺里面的设计 比如各种冷却器 需要满足条件才会工作那样的)

//还有一些双关语 还没想好 比如iridium 是Ir 同时 iridescent是多彩的 他们都来源于iris(彩虹女神) 这也是为什么开始时就写好名称了 但我还在计划 (参考铱的提炼 还有一些无厘头的东西)

//说说我的想法 首先逻辑层 增加多维列表  还有相关规则执行的函数（增加意味着我们保留前面框架的东西） 。 然后做一个对了 自走棋跟工厂自动化结合的游戏感觉怎么样？就像康威的生命游戏那种元胞自动机 但是 把规则改成更贴近自走棋那种 什么羁绊之类的东西（比如当周围有多少个 有xx标签 羁绊的实体时 怎么工作 积累工作进度 改变其他实体等等） 让其贴近现代流行游戏范式（避免理解困难 毕竟康威的生命游戏本身门槛就很高了） 。首先是关于设计 我估计 在js13k后会持续开发 直到完成。 第二是胜利，有胜利触发的实体 让其完成充能  就是胜利（这里套娃点就是 那些实体 会有其对应的条件 然后不断的下放） 第三是快照 更新下一刻是更新快照 然后快照取代目前这一刻（不会有更新先后顺序问题 因为改变全部集中在下一帧 而这一帧的状态是固定了的） 第四是设计 设计确实困难。 我想了想一些例子 比如 实体有atp 和atp前驱 然后 有酶 把atp换成atp前驱 并把对应的原料转化为产物（原料 产物也都是实体）然后还有自动扩散 类似的 还有其他比如把atp前驱换成atp的实体。（那些微观过程 其实都非常适合参考 往后会换皮来贴合js13k的主题 ）准确说源还是什么 本质上我是增加某种守恒量 只有一些节点（当然也是实体）才能添加 ，以避免左脚踩右脚上天 就像 钞票的面值随意改写 并不会真的增加社会上的生产力一样， 那些底层限制 也有许多皮肤 比如 勇者下地牢（地牢实体和冒险家 城镇实体） 比如 矿物（物料） 还有能量 什么的 。 另外 在js13k中 没有放置成本或者拆除成本 实际上这个游戏就没有 什么仓库的设定（而是物品皆实体 资源皆实体 玩家放置 与破坏 只是逻辑层里面的命令 ）我再详细说一下规则 规则其实 就是每个实体的标签（词条） 总的来说 一个词条的设计就是一个完整的函数 算法 把这个词条的任务完成。 词条间可能有交互作用。 举个例子 在一次更新中 一个工厂 先看视力词条（比如 检查曼哈顿距离在一个集合x中的实体 然后记录下来 比如看到五个铁矿 ，两个发电厂（准确说是在运行的 这就是前面说的记录 这有点含糊是因为 他是一个跟具体需求相关是内容）  然后 接下来 其他词条 比如冶铁 一看前面视力给这个实体的东西 就判断条件 说可以加工 然后得到一些铁 铁可能是工厂的词条 比如存储的 也可能直接变成世界上的实体 等等 这样的 ）更准确的说 实体是提交了命令 提交他们的意图 而不是直接执行 这样我们就能 做些判断 比如 发电功率 和耗电功率是否匹配？ 有没有一物 两用的情况（而在快照式更新的情况下 被消耗品并不能及时告知消费者 所以可能有这种bug存在） 总之就是 实体也不准直接改世界 而是提交命令 统一管理

(2026/9/3)

