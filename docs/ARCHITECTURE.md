//English ver coming soon.

//不要问我什么语言，我怎么写就是什么语言(雾)

//先只考虑无头游戏(headless game) 这是底层逻辑 让一个游戏可玩 （按理说这样的游戏可以通过内存探针 直接读写内存实现游玩 这是一个思想实验 我们所谓的渲染与输入 不过是这个探针的配套程序 让其面向用户)

//而后再考虑I/O。

首先第一步是启动:

&#x09;做初始化//比如加载SDF素材到png （此举旨在利用png的便利)

&#x09;进入第一个大循环(True):

&#x09;	一些东西（没想好,跟输入音频有关)

&#x09;	是否进入新游戏？

&#x09;		是：

&#x09;			去生成地图，

&#x09;			进小循环

&#x09;		否：

&#x09;			进小循环

&#x09;	//（小循环退出后 比如游戏通关或者重开 则回到大循环准备下一把）



小循环：

&#x09;逻辑更新器:

逻辑更新器:

&#x09;如果暂停模式 则直接返回:

&#x09;	把SPEED加到一个池中(无意义的值直接视作1)

&#x09;	如果该池大于1进入循环:

&#x09;		如果自动暂停开启，检查当前逻辑公式如果逻辑公式成立则跳过，同时清空池并设置暂停模式为真（防止速度异常)

&#x09;		执行一个逻辑帧

&#x09;		池减少1

//上面有暂停模式的变量 为真就认为是暂停，反之认为在自动模式。 自动模式下如果自动暂停启动 则根据其选择的公式进行判定，满足时自动暂停， 公式可以配置，而其中的布尔变量来源于计数器和监视器。

//计数器和监视器是未来信号网络的设计 目前只考虑一个计数器 每次执行逻辑帧后加1 大于N时（N可以设置）触发 并清空N。

//至此 基本上完成了无头游戏框架 然鹅 具体的游戏内核并未填充。

//我们继续考虑框架，考虑一个输入层(INPUT)， 负责把事件等翻译成命令送入命令队列（这意味着需要在前面增加命令队列的位置 以便这些命令去修改内存）

//我们再考虑一下 当打开了一个菜单后 有更多选项出现 意味着 本来输入层就是具备了状态 以实现复合输入（比如选择一个实体 然后在抚摸 聊天 xxx yyy等行为中选一个)

//然后我们再去考虑渲染层 渲染层第一步要读取游戏内的东西 以实现渲染画面 第二步需要读取输入层的状态 （该状态告诉渲染层 应该渲染什么按钮 怎么处理碰撞）

//至此 逻辑完善 关键是细节 明天再搞(2026/9/1) (另外一提 音频 还有素材等实际上是渲染层干的 音频有点独立  音频和美术还有游戏逻辑可以并行开发)

